import { Card } from 'antd';
import styled from 'styled-components';

export default styled(Card)`
  border-radius: 10px;
  min-width: calc(320 - 32) px;
  overflow: hidden;
  img {
    width: 100%;
  }
`;
