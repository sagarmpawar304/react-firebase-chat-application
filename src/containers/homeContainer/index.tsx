import React from 'react';
import { CustomCard } from '../../components/CustomCards/CustomCard';

const Home = () => {
  return (
    <div>
      <CustomCard num={35} />
    </div>
  );
};

export default Home;
